name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Build Assembler
      run: dotnet build 
      working-directory: ./DesignYourOwnCpu/Assembler
    - name: Build Emulator
      run: dotnet build 
      working-directory: ./DesignYourOwnCpu/Emulator
    - name: Test Emulator and Assembler
      run: dotnet test ./DesignYourOwnCpu.sln
      working-directory: ./DesignYourOwnCpu
